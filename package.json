{
  "name": "algiedi",
  "version": "0.0.1",
  "author": "Yue Chang",
  "license": "ISC",
  "scripts": {
    "check:frontend": "npm --prefix ./frontend/ run pre:commit",
    "check:backend": "cd backend && docker-compose -f docker-compose.builder.yml run --rm pre-commit",
    "pre:commit": "run-p check:*",
    "fix:frontend": "npm --prefix ./frontend/ run fix",
    "fix:backend": "cd backend && docker-compose -f docker-compose.builder.yml run --rm fix",
    "fix": "run-p fix:*",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.0",
    "npm-run-all": "^4.1.5"
  }
}
