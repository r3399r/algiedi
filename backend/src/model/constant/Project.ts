export enum Status {
  Created = 'created',
  InProgress = 'in-progress',
  Published = 'published',
}

export enum Role {
  Owner = 'owner',
  Inspiration = 'inspiration',
  Collaborator = 'collaborator',
  Rejected = 'rejected',
}
